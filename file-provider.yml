---
http:
  middlewares:
    keycloak:
      forwardAuth:
        address: "http://traefik_forward_auth:4181"
        trustForwardHeader: true
        authResponseHeaders:
          - X-Forwarded-User
